Fragment é um elemento usuado no lugar da div em um componente HTML, seu intuito é desaparecer quando o codigo for compilado, deixando o codigo com menos divs

se for usar importando, 
import Fragment ...
e usar a tag <Fragment></Fragment>

mas é possivel usar ele sem importar, e usar apenas <></>

entre o codigo como elemento pai, pois o codigo ja vai entender que é um fragment